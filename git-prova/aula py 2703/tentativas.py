


#alunos = [['vanessa','pietro','janaína','Gregório','luan'], [5.0, 7.5, 10.0, 6.0, 7.5]]
#print(alunos[0][3])
#print(alunos[1][3])

#del alunos [0][1]
#del alunos [1][1]
#print(alunos)

#alunos[1].append('10.0')
#alunos[0].append('Ricardo')
#print(alunos)


#import math

# Entrada dos coeficientes (como números de ponto flutuante)
#a = float(input("Digite o valor de a: "))
#b = float(input("Digite o valor de b: "))
#c = float(input("Digite o valor de c: "))

# Cálculo do discriminante (Delta)
#delta = b**2 - 4*a*c

# Verifica a natureza das raízes de acordo com o valor de Delta
#if delta > 0:
    # Duas raízes reais e distintas
    #x1 = (-b + math.sqrt(delta)) / (2*a)
    #x2 = (-b - math.sqrt(delta)) / (2*a)
    #print(f"As raízes da equação são: x1 = {x1} e x2 = {x2}")
#elif delta == 0:
    # Raiz real única (dupla)
    #x = -b / (2*a)
    #print(f"A equação possui uma única raiz real: x = {x}")
#else:
    # Delta < 0, não há raízes reais
    #print("A equação não possui raízes reais (Delta < 0).")
    
    
!pip install ColabTurtlePlus
from ColabTurtlePlus.Turtle import *
